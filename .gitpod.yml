image: gitpod/workspace-mysql

tasks:
  - init: "docker-compose up -d"
  - init: mysql -u root -e "CREATE DATABASE brazilian_ecommerce"

vscode:
    extensions:
      - vscjava.vscode-java-pack
      - humao.rest-client
      - vscjava.vscode-gradle

ports:
  - port: 8080
    visibility: public